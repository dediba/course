{
  "name": "Coordinates",
  "version": "0.0.1",
  "dependencies": {
    "leaflet": "^1.0.3",
    "reload": "^1.1.1",
    "ts-loader": "^2.0.0"
  },
  "devDependencies": {
    "reload": "^1.1.1"
  },
  "scripts": {
    "onchange": "onchange './lesson2/src/**/*.*' -- npm run move",
    "dev": "tsc -w | npm run onchange| reload -b -d './lesson2/dist' ",
    "move": "sudo cp -f ./lesson2/src/*.html ./lesson2/dist/ && sudo cp -f ./lesson2/src/*.css ./lesson2/dist/"
  }
}
